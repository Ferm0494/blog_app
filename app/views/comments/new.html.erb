<h2>New Comment for: <%= @post.title %></h2>
<%=render 'shared/error',local: @comment %>
<%=form_with(url: post_comments_path,local:true) do |f|%>
    <%=f.label "Comment"%>
    <%=f.text_field :body  %>
    <%= f.submit "Create comment" %>

<%end%>
